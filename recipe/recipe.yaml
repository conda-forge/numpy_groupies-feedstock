schema_version: 1

context:
  version: "0.12.2"

package:
  name: numpy_groupies
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/n/numpy_groupies/numpy_groupies-${{ version }}.tar.gz
  sha256: 8934562b0dace01683f741f0dd378d0da40814c16b5a56e4ffd17e0a99c16cb4

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools >=77
    - setuptools-scm >=8
    - pip
  run:
    - python >=${{ python_min }}
    - numpy

tests:
  - python:
      imports:
        - numpy_groupies
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  summary: "Optimised tools for group-indexing operations: aggregated sum and more."
  license: BSD-2-Clause
  license_file: LICENSE.txt
  homepage: https://pypi.org/project/numpy-groupies/

extra:
  recipe-maintainers:
    - dcherian
    - ml31415
    - ocefpaf
    - synapticarbors
